# 本地文件语义索引系统设计方案

> 定位：不重写文件系统，而是给现有文件系统建一层"影子索引"——用本地大模型把任意类型文件的内容转成可搜索的文本/向量/实体，存入本地数据库，支持关键词、语义、自然语言三种检索方式。默认配置使用回环地址上的本地模型；若用户将某项能力的 OpenAI 兼容 `base_url` 改为远程服务，该能力处理的文件内容会发送到该服务。

---

## 1. 目标与非目标

**目标**
- 监控用户指定的若干文件夹，对其中的文件建立内容索引（而非仅文件名索引）
- 不同类型文件用不同策略提取内容：文本直存、图片用本地视觉模型描述、其他格式用 Agent + 工具或自定义脚本
- 支持关键词全文搜索、语义搜索、自然语言 Agent 搜索
- 默认使用本地模型（通过 OpenAI 兼容接口，如 Ollama / llama.cpp server）；也可由用户显式改为远程兼容服务，并了解相应内容会离开本机

**非目标（至少 MVP 阶段不做）**
- 不接管文件系统底层存储，不改变文件本身的读写方式（这是当年 WinFS 真正失败的部分）
- 不做多用户权限体系，先做单用户桌面工具
- 不做实时协作 / 云同步

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户界面层                                │
│   搜索框 (关键词/语义)  |  自然语言问答框  |  索引状态/设置面板         │
└───────────────┬─────────────────────────────┬─────────────────────┘
                │                             │
        ┌───────▼────────┐          ┌─────────▼─────────┐
        │   检索引擎        │          │   Agent 搜索层       │
        │ FTS5 + 向量混合排序│          │ 本地LLM + 工具调用   │
        └───────┬────────┘          └─────────┬─────────┘
                │                             │
                └───────────┬─────────────────┘
                            │
                    ┌───────▼────────┐
                    │   SQLite 数据库   │
                    │ files/chunks/    │
                    │ entities/config  │
                    └───────▲────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼──────┐   ┌────────▼───────┐   ┌────────▼────────┐
│ 文件系统监听器  │   │  索引调度队列    │   │  内容提取器（插件化） │
│ FSEvents/     │──▶│ 去重/优先级/并发 │──▶│ 分层策略见第4节      │
│ inotify       │   │ 限流            │   └────────┬────────┘
└───────────────┘   └────────────────┘            │
                                          ┌─────────▼─────────┐
                                          │   本地模型服务层     │
                                          │ LLM / 视觉 / Embedding│
                                          │ /OCR / ASR（Ollama等）│
                                          └────────────────────┘
```

---

## 3. 数据模型（SQLite）

```sql
-- 文件主表
CREATE TABLE files (
    id            INTEGER PRIMARY KEY,
    path          TEXT UNIQUE NOT NULL,
    filename      TEXT NOT NULL,
    ext           TEXT,
    mime_type     TEXT,
    size          INTEGER,
    mtime         INTEGER,
    content_hash  TEXT,              -- 用于判断内容是否变化，避免重复调模型
    extractor     TEXT,              -- 命中的提取器名称
    index_status  TEXT,              -- pending / indexing / done / failed / skipped
    error_msg     TEXT,
    indexed_at    INTEGER
);

-- 提取出的正文（可能很长，与 chunks 分开存）
CREATE TABLE contents (
    file_id       INTEGER PRIMARY KEY REFERENCES files(id),
    text          TEXT,              -- 全文/描述文本
    summary       TEXT,              -- 模型生成的摘要（可选，用于快速预览）
    language      TEXT
);

-- 全文检索虚拟表（FTS5，BM25）
CREATE VIRTUAL TABLE contents_fts USING fts5(
    text, summary, filename, content='contents', content_rowid='file_id'
);

-- 分块 + 向量（用 sqlite-vec 扩展）
CREATE TABLE chunks (
    id            INTEGER PRIMARY KEY,
    file_id       INTEGER REFERENCES files(id),
    chunk_index   INTEGER,
    text          TEXT
);
CREATE VIRTUAL TABLE chunk_vectors USING vec0(
    embedding float[768]             -- 维度按所选 embedding 模型调整
);

-- 实体与关系（进阶功能，见第7节）
CREATE TABLE entities (
    id            INTEGER PRIMARY KEY,
    name          TEXT,
    type          TEXT               -- person / project / org / date / tag ...
);
CREATE TABLE file_entities (
    file_id       INTEGER REFERENCES files(id),
    entity_id     INTEGER REFERENCES entities(id),
    context       TEXT               -- 命中片段，便于溯源
);

-- 用户手动标签（不依赖模型，随时可加）
CREATE TABLE tags (id INTEGER PRIMARY KEY, name TEXT UNIQUE);
CREATE TABLE file_tags (file_id INTEGER, tag_id INTEGER);

-- 提取器注册表（配置驱动，支持用户自定义脚本）
CREATE TABLE extractor_rules (
    id            INTEGER PRIMARY KEY,
    match_type    TEXT,              -- ext / mime / glob
    match_value   TEXT,
    extractor     TEXT,              -- builtin_text / builtin_pdf / vision_model /
                                      -- agent / custom_script
    script_path   TEXT,              -- custom_script 时使用
    priority      INTEGER
);
```

---

## 4. 内容提取策略（分层，确定性优先）

不建议所有非文本文件都丢给 Agent——太慢太贵。按优先级分四层，命中即返回：

| 层级 | 适用场景 | 方式 |
|---|---|---|
| ① 直存 | txt/md/json/代码/csv 等 | 直接读文件写入 `contents.text` |
| ② 确定性解析 | pdf/docx/xlsx/pptx/epub/zip 等结构化格式 | 用现成库抽文本（pdfplumber、python-docx、openpyxl 等），压缩包递归解析内部文件 |
| ③ 专用模型 | 图片、扫描件、音频、视频 | 图片→本地视觉模型描述；扫描 PDF→先 OCR（Tesseract，比调模型快且免费），OCR 质量差再退化到视觉模型；音频/视频→本地 ASR（faster-whisper / whisper.cpp）转录字幕再入库 |
| ④ Agent 兜底 | 层①②③都不认识的格式 | 给一个装备了工具的 Agent：`read_bytes(path, offset, len)`、`run_custom_script(path)`、`list_archive(path)`、`shell_exec(cmd)`（默认关闭，需用户显式为某类文件开启）。Agent 自行判断怎么读、读多少、要不要转文字 |
| ⑤ 用户自定义脚本 | 任意用户想自己控制的格式 | 在 `extractor_rules` 里注册：某扩展名 → 一段脚本（接收安全快照路径，输出文本，约定 stdout 协议），优先级高于内置层，可覆盖①②③ |

Agent 层的工具集合建议保持"只读 + 白名单"，避免 Agent 在索引过程中意外执行危险操作。

---

## 5. 本地模型服务层

LLM、视觉和 embedding 统一走 **OpenAI 兼容接口**（`base_url` + `api_key` + `model`），本地首选 [Ollama](https://ollama.com)。检索 Agent、实体抽取、未知文本兜底、视觉和 embedding 都可分别配置模型；旧版通用 LLM 配置仍可兼容读取。

| 能力 | 推荐本地模型（Ollama） | 说明 |
|---|---|---|
| 通用 LLM（Agent/自然语言搜索/摘要） | qwen2.5 / qwen3 系列、llama3.1 | 需支持 function calling 才能做 Agent 工具调用，Qwen 系列对中文友好 |
| 视觉模型（图片描述） | qwen2-vl / minicpm-v / llava | 中文场景优先 Qwen2-VL 或 MiniCPM-V |
| Embedding | bge-m3 / nomic-embed-text / mxbai-embed-large | bge-m3 中英文效果均衡，注意维度要和 `chunk_vectors` 表定义匹配 |
| OCR | Tesseract 或本地 HTTP 适配器 | 本地 HTTP 适配器可包装 PaddleOCR 等服务 |
| ASR | faster-whisper 或 OpenAI 兼容 Whisper | 音频/视频转录 |

配置层设计成"每个使用位置独立可配"，方便为问答、实体和兜底选择不同模型，也方便换本地模型不用改代码。OCR 的本地 HTTP 协议使用 multipart 上传 `file` 与 `languages` 并按可配置 JSON 点路径读取文本；ASR 使用标准 Whisper 转写协议。当前仍不提供按监控文件夹或文件类型分流到远程服务的策略；需要确保内容不出机器时，应只配置本地回环地址上的服务。

---

## 6. 增量索引机制

- 首次运行：全量扫描指定根目录（可配置排除规则，如 `.git`、`node_modules`、`__pycache__`、超过 N MB 的文件默认跳过）
- 后续运行：
  - macOS 用 `FSEvents`、Linux 用 `inotify` 做实时监听，文件变化（新增/修改/删除/重命名）直接入队
  - 长时间监听时默认每 24 小时做一次全量对账扫描，防漏（可通过 `watch.reconcile_sec` 配置或关闭）；对账时才重试失败/跳过项，避免事件高峰中反复重试
- 变化判定：`content_hash`（文件内容哈希）不同才重新提取，仅 `mtime` 变化但内容哈希相同则跳过，避免无谓重跑模型
- 当前索引管线串行处理变更，优先保证本地模型资源稳定；优先级队列和可调并发属于后续性能优化

---

## 7. 检索方式

### 7.1 关键词搜索
直接查 `contents_fts`（FTS5 BM25），支持文件名、正文、摘要联合检索。

### 7.2 语义搜索
query 用同一 embedding 模型编码，`chunk_vectors` 做余弦/点积近邻检索，返回命中的 chunk 及其所属文件。

### 7.3 混合检索（默认模式）
BM25 结果与向量检索结果做 **RRF（Reciprocal Rank Fusion）** 融合排序——单纯关键词搜索对模糊描述弱，单纯语义搜索对精确文件名/术语弱，混合效果显著更好。

### 7.4 自然语言 Agent 搜索
本地 LLM 装备检索工具，自己拆解查询、多步调用、组合过滤条件：

- `search_fulltext(query)`
- `search_semantic(query)`
- `filter_by_metadata(ext=, path_prefix=, mtime_range=, size_range=)`
- `search_by_entity(name)`（依赖第 8 节实体功能）
- `get_file_detail(path)`

例如用户问"上个月和张三那个项目有关的 PDF 都有哪些"，Agent 会拆成：`filter_by_metadata(ext=pdf, mtime_range=上月)` + `search_by_entity("张三")` 或语义检索"项目"，再合并结果给出答案和文件路径列表。

这样比端到端黑盒回答更可控、可解释、也更省 token（不用把所有候选文件内容都塞进 prompt）。

---

## 8. 实体与关系抽取（进阶，非 MVP 必需）

这是最接近 WinFS 原始愿景的部分——WinFS 真正想解决的不是"内容能搜到"，而是"文件之间、文件与人/项目/日期之间的关联"。

做法：索引正文之后，异步跑一次轻量实体抽取（复用本地 LLM，prompt 要求输出人名/项目名/机构/日期等结构化列表），写入 `entities` / `file_entities`。之后可以支持"找出所有跟某人相关的文件（不论是文档、邮件截图还是聊天记录）"这类查询，也可以在 UI 上做一个"实体视图"，反向从人/项目找文件。

建议放在 Phase 2，因为需要先有稳定的正文提取和检索能力打底。

---

## 9. 隐私与安全

- 默认配置使用本地模型，文件内容不出机器，这是相对云端方案（直接调 OpenAI API）最大的优势
- 可以将 LLM、视觉或 embedding 的 `base_url` 显式改为远程兼容服务；对应能力发送的文件内容、图片或查询会交给该服务。当前版本没有按文件夹或类型的远程出站控制，处理敏感目录时应保持这些地址为本机服务
- Agent 兜底层的 `shell_exec` 类工具默认关闭，需用户为特定文件类型显式授权
- 索引数据库本身建议放在用户目录下受保护位置，不做网络暴露；如果之后做成服务化/多端同步，需要重新设计权限模型（对齐原始文件的 OS 权限），MVP 阶段不处理

---

## 10. 技术栈建议

- 语言：Python 为主（生态对接本地模型、文档解析库最成熟：pypdf/pdfplumber、python-docx、openpyxl、faster-whisper、pytesseract 等）；文件监听可用 `watchdog` 库跨平台
- 存储：SQLite + `sqlite-vec` 扩展（向量）+ FTS5（全文），单文件、零运维、适合桌面工具
- 模型接入：统一走 OpenAI SDK 的 `base_url` 参数指向 Ollama（`http://localhost:11434/v1`）
- UI：MVP 阶段可以先做一个本地小型 Web 界面（FastAPI + 简单前端）或命令行搜索工具，验证链路后再考虑做成菜单栏 App / Spotlight 式呼出窗口

---

## 11. 实现状态

- **Phase 0**：已完成文本/PDF/现代 Office/图片 OCR、FTS5、命令行和本地网页；扫描 PDF 的 OCR 依赖 Tesseract + Poppler。
- **Phase 1**：已完成 embedding、混合排序、视觉描述，以及带受限工具调用和结构化回退的自然语言 Agent 搜索。
- **Phase 2**：已完成实体关系抽取、可选 faster-whisper / OpenAI 兼容 ASR、本地 HTTP OCR（可包装 PaddleOCR）、ZIP/邮件/legacy Office 与自定义脚本扩展口。
- **Phase 3**：已完成跨平台事件监听、多文件夹增量扫描和本地网页设置页；性能会随库规模继续迭代。
- **后续优化**：sqlite-vec 大规模近邻检索、更多二进制格式专用解析器、呼出式桌面窗口和可选云端后备。

---

## 12. 开放问题（需要后续决策）

1. 不同本地模型的原生工具调用稳定性不一；当前已经有结构化检索计划回退，但仍值得实测 Qwen/Ollama 的效果。
2. 大文件/超大文件夹的索引耗时与资源占用（本地模型吃 CPU/GPU），需要设置合理的默认限流与用户可见的索引进度。
3. 视觉模型对截图类图片（含大段文字的截图）效果如何，是否需要先过 OCR 再过视觉模型做补充描述。
4. 是否需要版本历史（文件被覆盖后，旧版本的索引要不要保留），当前仍采用覆盖式更新。
