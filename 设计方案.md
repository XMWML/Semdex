# 本地文件语义索引系统设计方案

> 定位：不重写文件系统，而是给现有文件系统建一层"影子索引"——用本地大模型把任意类型文件的内容转成可搜索的文本/向量/实体，存入本地数据库，支持关键词、语义、自然语言三种检索方式。默认配置使用回环地址上的本地模型；若用户将某项能力的 OpenAI 兼容 `base_url` 改为远程服务，该能力处理的文件内容会发送到该服务。

---

## 1. 目标与非目标

**目标**
- 监控用户指定的若干文件夹，对其中的文件建立内容索引（而非仅文件名索引）
- 每条扩展名规则显式选择直接索引文本、指定 LLM 或文件夹式 Python 插件；OCR/ASR 也走插件
- 支持关键词全文搜索、语义搜索、自然语言 Agent 搜索
- 默认使用本地模型（通过 OpenAI 兼容接口，如 Ollama / llama.cpp server）；也可由用户显式改为远程兼容服务，并了解相应内容会离开本机

**非目标（至少 MVP 阶段不做）**
- 不接管文件系统底层存储，不改变文件本身的读写方式（这是当年 WinFS 真正失败的部分）
- 不做多用户权限体系，先做单用户桌面工具
- 不做实时协作 / 云同步

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户界面层                                │
│   搜索框 (关键词/语义)  |  自然语言问答框  |  索引状态/设置面板         │
└───────────────┬─────────────────────────────┬─────────────────────┘
                │                             │
        ┌───────▼────────┐          ┌─────────▼─────────┐
        │   检索引擎        │          │   Agent 搜索层       │
        │ FTS5 + 向量混合排序│          │ 本地LLM + 工具调用   │
        └───────┬────────┘          └─────────┬─────────┘
                │                             │
                └───────────┬─────────────────┘
                            │
                    ┌───────▼────────┐
                    │   SQLite 数据库   │
                    │ files/chunks/    │
                    │ entities/config  │
                    └───────▲────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼──────┐   ┌────────▼───────┐   ┌────────▼────────┐
│ 文件系统监听器  │   │  索引调度队列    │   │  内容提取器（插件化） │
│ FSEvents/     │──▶│ 去重/优先级/并发 │──▶│ 分层策略见第4节      │
│ inotify       │   │ 限流            │   └────────┬────────┘
└───────────────┘   └────────────────┘            │
                                          ┌─────────▼─────────┐
                                          │   模型与插件能力层    │
                                          │ 一级LLM / Agent /     │
                                          │ Embedding / OCR / ASR │
                                          └────────────────────┘
```

---

## 3. 数据模型（SQLite）

当前实现的核心表可简化表示为：

```sql
-- 文件主表
CREATE TABLE files (
    id            INTEGER PRIMARY KEY,
    path          TEXT UNIQUE NOT NULL,
    filename      TEXT NOT NULL,
    ext           TEXT,
    size          INTEGER,
    mtime         REAL,
    content_hash  TEXT,              -- 用于判断内容是否变化，避免重复调模型
    extractor     TEXT,              -- 命中的提取器名称
    index_status  TEXT,              -- pending / indexing / done / failed / skipped / waiting_*
    error_msg     TEXT,
    indexed_at    REAL,
    entity_status TEXT,              -- pending / done / failed / waiting_model
    entity_error  TEXT
);

-- 一级索引生成的正文；向量和实体都从这里继续建立
CREATE TABLE contents (
    file_id       INTEGER PRIMARY KEY REFERENCES files(id) ON DELETE CASCADE,
    text          TEXT
);

-- 全文检索虚拟表（FTS5，BM25）
CREATE VIRTUAL TABLE contents_fts USING fts5(
    text, filename, tokenize='unicode61'
);

-- 正文分块与向量；当前向量为 float32 BLOB，由 NumPy 计算余弦
CREATE TABLE chunks (
    id            INTEGER PRIMARY KEY,
    file_id       INTEGER REFERENCES files(id) ON DELETE CASCADE,
    chunk_index   INTEGER,
    text          TEXT,
    embedding     BLOB
);

-- 实体与关系（可选功能，见第 8 节）
CREATE TABLE entities (
    id              INTEGER PRIMARY KEY,
    name            TEXT,
    normalized_name TEXT,
    type            TEXT,
    UNIQUE(normalized_name, type)
);
CREATE TABLE file_entities (
    file_id       INTEGER REFERENCES files(id) ON DELETE CASCADE,
    entity_id     INTEGER REFERENCES entities(id) ON DELETE CASCADE,
    context       TEXT,
    PRIMARY KEY(file_id, entity_id)
);

-- 持久化索引身份、向量重建状态等内部元数据
CREATE TABLE meta (
    key           TEXT PRIMARY KEY,
    value         TEXT
);
```

扩展名规则、LLM 供应商和各独立模型连接保存在 `.semdex/config.toml`，不放入数据库注册表；WebUI、PySide6 原生 UI、CLI 和 watcher 都读取同一份配置并复用同一套索引状态同步逻辑。

---

## 4. 一级正文索引策略

每个扩展名显式选择一种一级索引方式，不再通过隐藏的图片理解或未知格式兜底分支猜测：

| 方式 | 适用场景 | 行为 |
|---|---|---|
| 直接索引文本 | txt/md/json/代码，以及 PDF/Office/邮件/压缩包等已知格式 | 文本直接读取；结构化格式使用确定性解析器，结果写入 `contents.text` |
| 传入 LLM | 需要摘要、字段整理或多模态理解的格式 | 规则选择稳定的 LLM 供应商 ID、文本/图片输入和专属 Prompt；文本模式先执行确定性解析，图片模式传入白名单内的原始图片 |
| Python 外置插件 | OCR、ASR、私有或项目专用格式 | `extractors/<插件名>/plugin.py` 提供 `extract(path)` 或 `extract(path, ctx)`，在安全快照上执行并返回一级正文 |

内置规则不能删除，但扩展名、开关和方式可以调整；用户规则可以增删。同一扩展名不能同时出现在多条已启用规则中。图片输入只允许 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`、`.bmp`，配置校验和两套设置界面都会拒绝其他格式。OCR 与 ASR 是随附插件，而不是索引器里的特殊分支。旧版单文件 Python 和 `match + script` 规则只作为迁移兼容保留。

这是严格分层的索引管线：一级路由先把原文件转换成 `contents.text` 并更新 FTS；只有一级正文完成后，才会从这份正文生成 chunks/embedding，或交给实体模型写入 `entities` / `file_entities`。向量和实体不绕过一级正文直接读取原文件。

---

## 5. 本地模型服务层

一级索引 LLM、检索 Agent、实体抽取和 embedding 可走 **OpenAI 兼容接口**（`base_url` + `api_key` + `model`）或项目内模型。一级索引维护可增删的供应商列表；检索 Agent、实体抽取和 embedding 是三个独立连接。旧版通用、视觉和兜底模型配置只用于迁移读取，不再出现在新设置中。

| 能力 | 推荐本地模型（Ollama） | 说明 |
|---|---|---|
| 一级索引 / Agent 文本 LLM | qwen2.5 / qwen3 系列、llama3.1 | Agent 最好支持 function calling；一级规则可使用独立 Prompt |
| 一级索引图片输入 / Agent 原图查看 | qwen2-vl / minicpm-v / llava | 只处理受支持图片；Agent 看图复用自己的模型，不另设图片理解模型 |
| Embedding | bge-m3 / nomic-embed-text / mxbai-embed-large | bge-m3 中英文效果均衡；同一索引内的向量维度必须一致 |
| OCR | Tesseract 或本地 HTTP 适配器 | 本地 HTTP 适配器可包装 PaddleOCR 等服务 |
| ASR | faster-whisper 或 OpenAI 兼容 Whisper | 音频/视频转录 |

配置层按使用位置独立：扩展名规则引用一级 LLM 供应商，问答、实体和向量各用自己的模型；`inspect_image` 复用检索 Agent 连接及其视觉能力。OCR 的本地 HTTP 协议使用 multipart 上传 `file` 与 `languages` 并按可配置 JSON 点路径读取文本；ASR 使用标准 Whisper 转写协议，这些参数由随附插件读取。当前仍不提供按监控文件夹分流到远程服务的策略；需要确保内容不出机器时，应只配置本地回环地址上的服务。

---

## 6. 增量索引机制

- 首次运行：全量扫描指定根目录（可配置排除规则，如 `.git`、`node_modules`、`__pycache__`、超过 N MB 的文件默认跳过）
- 后续运行：
  - macOS 用 `FSEvents`、Linux 用 `inotify` 做实时监听，文件变化（新增/修改/删除/重命名）直接入队
  - 长时间监听时默认每 24 小时做一次全量对账扫描，防漏（可通过 `watch.reconcile_sec` 配置或关闭）；对账时才重试失败/跳过项，避免事件高峰中反复重试
- 变化判定：`content_hash`（文件内容哈希）不同才重新提取，仅 `mtime` 变化但内容哈希相同则跳过，避免无谓重跑模型
- 一级规则、LLM 供应商、插件目录或源码、OCR/ASR 参数形成持久化配置指纹；这些输入变化时已有正文会自动重新入队。Embedding 和实体配置各自有独立指纹，只重建对应派生索引
- 当前索引管线串行处理变更，优先保证本地模型资源稳定；优先级队列和可调并发属于后续性能优化

---

## 7. 检索方式

### 7.1 关键词搜索
直接查 `contents_fts`（FTS5 BM25），支持文件名与正文联合检索。

### 7.2 语义搜索
query 用同一 embedding 模型编码，再与 `chunks.embedding` 中的向量计算余弦相似度，返回命中的 chunk 及其所属文件。

### 7.3 混合检索（默认模式）
BM25 结果与向量检索结果做 **RRF（Reciprocal Rank Fusion）** 融合排序——单纯关键词搜索对模糊描述弱，单纯语义搜索对精确文件名/术语弱，混合效果显著更好。

### 7.4 自然语言 Agent 搜索
检索 LLM 装备受限工具，自己拆解查询、多步调用、组合过滤条件：

- `search_fulltext(query)`
- `search_semantic(query)`
- `filter_by_metadata(ext=, path_prefix=, mtime_range=, size_range=, file_ids=)`
- `search_by_entity(name, file_ids=)`（仅在第 8 节实体功能开启时提供）
- `get_file_detail(file_id)`
- `inspect_image(file_id, question)`

例如用户问"上个月和张三那个项目有关的 PDF 都有哪些"，Agent 会拆成：`filter_by_metadata(ext=pdf, mtime_range=上月)` + `search_by_entity("张三")` 或语义检索"项目"，再合并结果给出答案和文件路径列表。

文件详情和图片查看不能凭空指定路径：`file_id` 必须先由本轮对话中的检索或筛选工具返回。`inspect_image` 还会要求文件已完成索引、扩展名属于 `.png` / `.jpg` / `.jpeg` / `.webp` / `.gif` / `.bmp`、文件头符合对应格式、解析后的路径仍在配置的索引根目录内，并拒绝路径中的符号链接。它复用检索 Agent 模型读取安全快照，模型不具备视觉能力时会返回工具错误，而不会恢复独立的“图片理解模型”。

实体关闭时，传给 LLM 的工具定义里完全移除 `search_by_entity`，文件详情也不返回实体列表。这样比端到端黑盒回答更可控、可解释、也更省 token（不用把所有候选文件内容都塞进 prompt）。

---

## 8. 实体与关系抽取（可选）

这是最接近 WinFS 原始愿景的部分——WinFS 真正想解决的不是"内容能搜到"，而是"文件之间、文件与人/项目/日期之间的关联"。

做法：一级正文写入后，使用独立配置的实体模型提取人名、项目名、机构、日期、地点和标签等结构化列表，写入 `entities` / `file_entities`。模型或抽取参数变化会自动清除并重建旧关系；关闭实体功能会清除旧关系，同时让 Agent 不再获得实体检索工具。一级正文和关键词搜索始终独立保留。

---

## 9. 隐私与安全

- 默认配置使用本地模型，文件内容不出机器，这是相对云端方案（直接调 OpenAI API）最大的优势
- 可以将一级 LLM 供应商或 Agent/实体/embedding 的 `base_url` 显式改为远程兼容服务；对应规则的文件内容、图片或查询会交给该服务。当前版本没有按文件夹的远程出站控制，处理敏感目录时应保持这些地址为本机服务
- Python 插件会执行本地代码，只应放入可信插件；插件拿到的是同名同扩展名的安全快照，而不是监控目录中的原始路径
- Agent 的图片工具只能查看本轮检索结果中的白名单图片，并在读取前再次校验索引根目录和符号链接；它不接受任意路径。不过 Agent 使用远程接口时，被查看的原图仍会发送到该接口
- 索引数据库本身建议放在用户目录下受保护位置，不做网络暴露；如果之后做成服务化/多端同步，需要重新设计权限模型（对齐原始文件的 OS 权限），MVP 阶段不处理

---

## 10. 当前技术栈

- 语言：Python 为主（生态对接本地模型、文档解析库最成熟：pypdf/pdfplumber、python-docx、openpyxl、faster-whisper、pytesseract 等）；文件监听可用 `watchdog` 库跨平台
- 存储：SQLite + FTS5；向量以 float32 BLOB 放在 `chunks.embedding`，当前由 NumPy 暴力余弦检索，后续可替换为 sqlite-vec
- 模型接入：OpenAI SDK 对接官方或兼容 `base_url`，也支持项目目录内的 GGUF/MLX/Whisper 模型并按能力加载、卸载
- UI：FastAPI WebUI 与 PySide6 原生 UI 两套界面，共用配置、数据库和核心服务；另提供 CLI

---

## 11. 实现状态

- **Phase 0**：已完成文本/PDF/现代 Office/图片 OCR、FTS5、命令行和本地网页；扫描 PDF 的 OCR 依赖 Tesseract + Poppler。
- **Phase 1**：已完成 embedding、混合排序，以及带受限工具调用、候选约束、图片查看和结构化回退的自然语言 Agent 搜索。
- **Phase 2**：已完成实体关系抽取、可选 faster-whisper / OpenAI 兼容 ASR、本地 HTTP OCR（可包装 PaddleOCR）、ZIP/邮件/legacy Office 与文件夹式 Python 插件扩展口。
- **Phase 3**：已完成跨平台事件监听、多文件夹增量扫描、主页进度、详细状态页，以及设置能力同步的 WebUI/PySide6 原生 UI；性能会随库规模继续迭代。
- **后续优化**：sqlite-vec 大规模近邻检索、更多二进制格式专用解析器、呼出式桌面窗口和可选云端后备。

---

## 12. 开放问题（需要后续决策）

1. 不同本地模型的原生工具调用稳定性不一；当前已经有结构化检索计划回退，但仍值得实测 Qwen/Ollama 的效果。
2. 大文件/超大文件夹的索引耗时与资源占用（本地模型吃 CPU/GPU）仍需继续测量，再决定并发、队列优先级和限流默认值。
3. 图片规则选择 OCR 插件或多模态 LLM 的效果差异，是否需要提供可组合的插件管线。
4. 是否需要版本历史（文件被覆盖后，旧版本的索引要不要保留），当前仍采用覆盖式更新。
