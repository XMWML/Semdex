# Semdex 使用说明

Semdex 是运行在本机的文件内容索引工具。它不会移动原文件；索引结果保存在 SQLite，搜索结果会回到原始文件路径。支持 macOS 和 Linux，并提供原生 UI 与 WebUI 两种界面。

## 1. 安装与启动

先安装 Python 3.11+ 和 [uv](https://docs.astral.sh/uv/)。在项目根目录执行启动脚本，脚本会在项目内同步依赖、保存缓存并启动界面：

```bash
# macOS：双击脚本，或在终端运行
./Start\ Semdex.command --ui native
./Start\ Semdex.command --ui web

# Linux
./Start\ Semdex.sh --ui native
./Start\ Semdex.sh --ui web
```

也可以使用跨平台 Python 启动器：

```bash
python3 "Start Semdex.py" --ui web
python3 "Start Semdex.py" --ui native
```

首次启动会创建项目内 `.semdex/config.toml`。WebUI 默认地址是 `http://127.0.0.1:8787`；原生 UI 不启动 HTTP 服务，但两种界面共享同一份配置、SQLite 索引、搜索、索引、模型管理和设置能力。网页服务只允许绑定回环地址，不提供未经认证的局域网或公网监听。

在设置页的“索引范围”中添加一个或多个目录，点击“保存并开始索引”。命令行等价操作是：

```bash
uv run semdex index
uv run semdex watch
```

## 2. 设置页

设置页包含以下区域：

- 索引范围：目录、排除规则、单文件大小上限、监听对账间隔和分块参数。
- 一级索引：每条扩展名规则选择“直接索引文本”“传入 LLM”或“Python 外置插件”。
- LLM 供应商：可添加、重命名和删除多个本地或云端供应商，供一级索引规则引用。
- 独立模型：检索 Agent、实体抽取、语义嵌入分别选择 `OpenAI API` 或本地模型，并附有用途说明。
- 随附插件参数：配置 `ocr/plugin.py` 与 `asr/plugin.py` 使用的 OCR、Whisper 或兼容 API。
- 二级索引开关：RAG 语义检索、实体关系和 LLM 工具搜索；三者都在一级正文索引上工作。

四个模型用途彼此独立，设置页中的说明对应如下：

| 设置项 | 用途与调用位置 |
|---|---|
| 一级索引 LLM 供应商 | 只处理选择 `llm` 的扩展名规则：把文本整理为可检索正文，或理解白名单图片；每条规则可选不同供应商和 Prompt。 |
| 检索 Agent | 回答自然语言搜索问题，调用全文、语义、元数据和受限文件详情工具；`inspect_image` 也复用这个模型。 |
| 实体抽取 | 在一级正文完成后抽取人名、项目、机构、日期、地点和标签，建立文件与实体关系；不负责一级正文。 |
| 语义嵌入 | 在一级正文上分块并生成向量，供语义/RAG/混合检索使用；更换模型或分块参数会重建向量。 |

选择本地模型时，模型必须位于项目模型目录内；选择 API 时填写该用途自己的地址、模型名和密钥。保存会校验目录、数据库、分块参数和启用项的必要字段。API Key 不会回显；留空表示保留旧值，勾选“删除已保存密钥”可清除。

## 3. 模型调用方式

### OpenAI API

每个用途都支持官方 OpenAI API 或 OpenAI 兼容服务。填写 `base_url`、`model` 和可选 `api_key`，例如：

```text
https://api.openai.com/v1
http://127.0.0.1:1234/v1       # LM Studio 等兼容服务
http://127.0.0.1:11434/v1      # Ollama 兼容接口
```

各用途所需协议如下：

| 用途 | 接口 |
|---|---|
| 一级索引 LLM、检索 Agent、实体抽取 | `POST /chat/completions` |
| 一级索引 LLM 的图片模式、Agent 的图片查看 | 支持图片输入的 `POST /chat/completions` |
| 语义嵌入 | `POST /embeddings` |

旧配置中的 `[models.llm]`、`[models.fallback]` 和 `[models.vision]` 仍可读取并迁移；新配置把一级索引连接写入 `[llm_providers]`，只在 `[models.agent]`、`[models.entities]` 和 `[models.embedding]` 保留独立模型。

原始图片输入统一只支持 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`、`.bmp`。一级索引规则若选择“原始图片”，所填扩展名必须全部属于这个列表；PDF、文档和其他格式应改选文本输入或 Python 插件。检索 Agent 需要查看原图时也使用同一格式范围，并要求 `[models.agent]` 选择的本地模型或 API 具备图片输入能力。

### 项目本地模型

默认模型目录是 `.semdex/models/`，对应 `.semdex/config.toml` 中的：

```toml
[storage]
model_dir = "models"
```

也可以在设置页查看实际目录。将模型文件或完整模型目录放入该目录后，点击“刷新模型”；每张用途卡片会只列出与该用途匹配的模型。模型 ID 是相对于 `.semdex/models/` 的路径，不支持目录外的绝对路径或 `..` 路径。

支持的布局：

| 格式 | 放置方式 | 用途 | 平台 |
|---|---|---|---|
| GGUF | 一个 `.gguf` 文件，可放在子目录中 | 文本、embedding；文件名含 `whisper` 时可用于 ASR | macOS / Linux |
| MLX | 含 `config.json`、tokenizer 文件和全部 `.safetensors` 分片的完整目录 | 文本、embedding、图片输入 LLM、MLX Whisper ASR | Apple Silicon macOS |
| faster-whisper | 含 `config.json`、`model.bin` 和 tokenizer 的 CTranslate2 目录 | ASR | macOS / Linux |
| GGML/Whisper.cpp | `.bin`/`.gguf` Whisper 文件（文件名以 `ggml-` 开头或含 `whisper`） | ASR | macOS / Linux |

当前 GGUF 后端使用 `llama-cpp-python`，提供文本和 embedding；模型本身必须支持所选能力。图片输入的本地 LLM 使用 MLX VLM，Linux 上请使用图片消息兼容的 OpenAI API。MLX 模型需要 Apple Silicon，Intel macOS 和 Linux 请使用 GGUF 或 API。

## 4. 下载和放置模型文件

模型权重不随源码发布，也不应提交到 Git。可从 Hugging Face 模型仓库下载。安装 Hugging Face CLI 后：

```bash
uv tool install huggingface_hub
```

下载 GGUF 单文件：

```bash
hf download <组织或用户名>/<仓库> <文件名>.gguf \
  --local-dir .semdex/models/<模型目录名>
```

下载 MLX 或 faster-whisper 完整目录时，保留仓库中的配置、tokenizer 和所有权重分片：

```bash
hf download mlx-community/<模型仓库> \
  --local-dir .semdex/models/<模型目录名>
```

示例布局：

```text
.semdex/models/
  qwen3-gguf/
    qwen3-8b-q4_k_m.gguf
  Qwen3-1.7B-MLX-8bit/
    config.json
    tokenizer.json
    model.safetensors
  whisper-large-v3-ct2/
    config.json
    model.bin
    tokenizer.json
```

如果仓库需要登录或接受许可，先执行 `hf auth login`。下载完成后回到设置页刷新模型库，确认格式、用途和运行时均可用。

语义嵌入模型可从 [Hugging Face 的 Feature Extraction 模型列表](https://huggingface.co/models?pipeline_tag=feature-extraction) 或 [ModelScope 模型库](https://modelscope.cn/models) 获取。GGUF 可以是模型目录中的任意单文件，但必须支持 embedding 输出；MLX 向量模型必须是完整文件夹，至少包含：

```text
.semdex/models/bge-m3-embedding/
  config.json
  tokenizer.json                 # 按模型仓库保留 tokenizer 相关文件
  model.safetensors              # 或全部 model-*.safetensors / consolidated.* 分片
```

MLX 文件夹名或 `config.json` 的模型信息须包含 `embedding`、`bge`、`e5`、`nomic`、`gte`、`jina` 之一，模型扫描器才会将它列为“向量”能力。设置页选择后，可在本地模型管理区以“向量”能力加载到内存或卸载；更换嵌入模型后，下一次普通索引会自动重建全部向量。

## 5. 安装本地运行时

基础启动只安装核心依赖。本地模型按需要安装额外运行时：

```bash
# macOS Apple Silicon：原生 UI + GGUF + MLX + ASR
./Start\ Semdex.command --ui native --with-local-models --with-asr

# macOS WebUI
./Start\ Semdex.command --ui web --with-local-models --with-asr

# Linux：原生 UI + GGUF + faster-whisper/Whisper.cpp
./Start\ Semdex.sh --ui native --with-gguf --with-asr

# 只同步依赖，不启动界面
python3 "Start Semdex.py" --with-local-models --with-asr --sync-only
```

参数说明：`--with-gguf` 安装 `llama-cpp-python` 和 `pywhispercpp`；`--with-mlx` 安装 MLX 文本、embedding、视觉和 Whisper 运行时（仅 macOS 生效）；`--with-asr` 安装 faster-whisper；`--with-local-models` 同时请求 GGUF 和 MLX 运行时。原生 UI 还需要 `gui` extra，启动脚本会自动添加。通过启动器安装的可选运行时会被保留，之后正常启动不会移除它们。

## 6. 加载、卸载和用途选择

在 WebUI 设置页的“项目模型库”或原生 UI 的“模型”页：

1. 点击“刷新模型”，确认模型目录、格式、用途和运行时状态。
2. 在对应用途卡片选择“项目本地模型”和具体模型；不同用途可以选择不同文件，也可以混合使用 API 与本地模型。
3. 在模型库中选择用途，点击“加载到内存”或“加载”。模型首次调用时也会自动加载。
4. 不再使用时点击“卸载所选能力”或“卸载”，释放该用途的内存；模型库支持查看已加载状态。

Whisper 的设置独立于其他模型：ASR 来源选择“本地模型”时，选择本地 Whisper 文件/目录及 `自动`、`faster-whisper`、`MLX Whisper` 或 `whisper.cpp` 后端；选择“OpenAI API”时填写转写接口。一个音频用途只能选择其中一种来源，但可与其他用途的 API/本地选择同时存在。

WebUI 对应接口：

```text
GET  /api/models
POST /api/models/load    {"model_id":"...", "capability":"chat|embedding|vision|asr"}
POST /api/models/unload  {"model_id":"...", "capability":"..."}
```

## 7. OCR、ASR 和常见工作流

### OCR

选择 Tesseract 时安装：

```bash
# macOS
brew install tesseract poppler
# Linux（以 Debian/Ubuntu 为例）
sudo apt install tesseract-ocr poppler-utils
```

中文识别还需要对应的 `chi_sim` 语言包。选择本地 HTTP 时，Semdex 以 multipart 的 `file`、`languages` 字段发送图片，并从 JSON 响应的 `text` 或自定义点路径读取文字；可用它适配 PaddleOCR 等服务。

### OpenAI API ASR

ASR 来源选择“OpenAI API”，填写 `base_url` 或完整 `endpoint`。默认请求：

```text
<base_url>/audio/transcriptions
```

请求包含 multipart 的 `file`、`model` 和可选 `language`，默认读取响应 JSON 的 `text` 字段，也可填写例如 `data.text`。

### 索引与降级

```bash
uv run semdex index
uv run semdex search "项目预算"
uv run semdex ask "上个月和张三有关的 PDF 在哪"
uv run semdex entities
uv run semdex embed --rebuild  # 可选：主动强制全量重建向量
```

模型未配置、运行时未安装或服务暂不可用时，其他扩展名的一级文本索引仍会继续；选择 LLM 的规则进入 `waiting_model`，OCR/ASR 插件缺少能力时进入 `waiting_capability`。能力恢复后再次运行 `semdex index`，或在设置页点击“保存并开始索引”即可补齐。文件总是先生成一级正文并写入全文索引，随后才在这份正文上分块生成向量、抽取实体并建立关系。首次启用 embedding，或更换模型、地址、分块大小与重叠量时，普通索引会自动补建或全量重建向量；失败会保留重试标记且不影响一级正文，完成前语义搜索退回关键词。

## 8. 手工配置、隐私和项目数据

可以运行 `uv run semdex config` 查看配置文件，也可以直接编辑 TOML。设置页的“一级索引”会保留不可删除的内置规则：每条均可开关、调整扩展名，并在以下三种方式中选择：

- 直接索引文本：文本直接读取；PDF、Office、压缩包和邮件使用对应的确定性解析器。
- 传入 LLM：选择下方任意 LLM 供应商、文本或图片输入方式，并为这一条规则填写专属 Prompt。文本模式先得到确定性正文再调用 LLM；图片模式要求供应商支持图片消息，并且规则只能包含 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`、`.bmp`。
- Python 外置插件：从 `extractors/<插件名>/plugin.py` 调用指定函数。函数接收安全快照 `path`，也可接收第二个 `ctx` 参数，返回字符串、字节串或其他可转成字符串的值。

例如：

```text
extractors/
  my_format/
    plugin.py
```

```python
from pathlib import Path

PLUGIN_METADATA = {
    "name": "我的格式",
    "description": "读取示例格式并返回一级正文",
    "function": "extract",
}

def extract(path: Path) -> str:
    return path.read_text(encoding="utf-8", errors="replace")
```

插件扫描只静态读取上述元数据，不会在打开设置页时导入代码；真正索引时才在安全快照上执行。OCR 与语音识别分别由随附的 `ocr/`、`asr/` 插件实现。旧版根目录单个 `.py` 文件及 `match + script` 可执行规则仍兼容，但新设置保存为文件夹插件格式。

主页的“状态”菜单可查看实时索引阶段和详细计数。RAG 语义检索由 `[rag]` 与 `[models.embedding]` 控制；LLM 工具搜索由 `[agent]` 与 `[models.agent]` 控制，模型可使用本地运行时或 OpenAI 兼容接口。

检索 Agent 只会收到受限工具，而不是任意文件系统或命令执行权限。它必须先调用全文、语义或元数据检索得到候选 `file_id`，之后才能用 `get_file_detail` 查看有限正文，或用 `inspect_image` 查看候选图片的原始内容。`inspect_image` 会再次检查图片已经完成索引、路径仍在当前配置的索引根目录内、路径中没有符号链接、扩展名属于图片白名单且文件头符合对应格式；不能传路径，也不能查看本轮对话未检索到的文件。它复用检索 Agent 模型，没有单独的“图片理解模型”设置，因此需要看图时请为 Agent 选择支持图片输入的模型。

实体关系关闭时，Agent 的工具列表中不会提供 `search_by_entity`；开启并完成实体抽取后才可按人名、项目、机构、日期、地点或标签继续收窄候选。向量或实体功能关闭不会影响已经建立的一级正文和关键词搜索。

当 API 的 `base_url`、ASR/OCR 的 `endpoint` 指向非回环地址时，对应文件内容、`inspect_image` 查看过的原图、音视频或查询会发送给该服务；敏感目录请只填写信任的服务地址。API Key 不会通过 `/api/settings` 返回。

默认配置、SQLite 索引及 WAL/SHM、临时文件、模型文件、Python 虚拟环境和 uv 缓存均在项目内的 `.semdex/`、`.venv/`、`.uv-cache/` 与 `.uv-python/`。移动项目后可运行：

```bash
python3 "Start Semdex.py" --sync-only
```

这些目录已在默认排除规则中，不会被自身索引；模型和运行时缓存不提交到仓库。
