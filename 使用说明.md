# Semdex 使用说明

Semdex 是运行在本机的文件内容索引工具。它不会移动原文件；索引结果保存在 SQLite，搜索结果会回到原始文件路径。

## 1. 启动与首次设置

```bash
cd ~/Desktop/Semdex
uv sync
uv run semdex serve
```

浏览器会打开 `http://127.0.0.1:8787`。第一次启动时，如果还没有配置文件，Semdex 会自动创建 `~/.semdex/config.toml`，随后直接打开右上角的“设置”。

在“索引范围”中逐个添加要监控的目录。可以添加多个目录，重复路径会自动合并。保存后点击“保存并开始索引”；以后文件变化可运行：

```bash
uv run semdex watch
```

网页只允许监听本机回环地址；非回环 `--host` 会被拒绝。设置接口可以修改模型服务地址和索引范围，因此当前版本不提供无认证的局域网或公网监听。

## 2. 设置页

设置页分成四组：

- 索引范围：目录列表、排除规则、文件大小上限、监听对账间隔和向量分块参数。
- 模型：检索 Agent、实体抽取、未知文本兜底、图片理解和语义嵌入均可选择不同的 OpenAI 兼容服务地址和模型名；“默认 LLM”用于兼容旧配置。
- OCR 与语音识别：OCR 可选 Tesseract 或本地 HTTP 服务；语音识别可选本机 faster-whisper 或 OpenAI 兼容转写服务。
- 功能开关：实体关系、未知文本兜底，以及每个模型、OCR 和 ASR 的启用状态。

保存时会验证目录是否存在、数据库路径可打开、分块重叠是否小于分块大小，以及启用服务的必要字段。索引运行期间不能保存配置，以免同一次索引混用两套设置。网页服务会立即使用新设置；若已在另一个终端运行 `semdex watch`，请停止并重新启动该监听进程，使其重新读取目录、模型和防抖配置。

网页不会读回或显示已保存的 API Key。输入框留空时会保留原有密钥；需要替换时再输入新的值。勾选对应密钥项下的“删除已保存密钥”并保存，可撤销该项凭据。

## 3. 模型用途

所有模型服务使用 OpenAI 兼容接口。LM Studio、Ollama 的 OpenAI 兼容模式、vLLM、llama.cpp server 等都可以填写对应的 `base_url`、`api_key` 和 `model`。

| 设置项 | 使用位置 | 需要的接口能力 |
|---|---|---|
| 检索 Agent | 网页“问答”和 `semdex ask` | Chat Completions，工具调用较好；不支持时会退化为结构化计划 |
| 实体抽取 | 人名、项目、机构、日期等关系 | Chat Completions |
| 提取兜底 | 未知但可读文本格式的摘要 | Chat Completions |
| 图片理解 | 图片的视觉描述 | 支持图片输入的 Chat Completions |
| 语义嵌入 | 语义/混合搜索 | `/embeddings` |

旧配置中的 `[models.llm]` 保留用于兼容；新设置中每个实际使用位置都有独立模型项。手工编辑 TOML 时，对应小节是 `[models.agent]`、`[models.entities]`、`[models.fallback]`、`[models.vision]` 和 `[models.embedding]`。

设置页的“默认 LLM”卡片是旧版 `[models.llm]` 的兼容默认项，不是第六个功能用途。新建配置时，请在需要的用途卡片中分别填写并启用模型；网页保存后会把这些用途分别写入配置，因此仅修改“默认 LLM”不会覆盖已经单独配置的用途模型。

实体关系需要同时开启“实体关系”功能和“实体抽取”模型；提取兜底也需要同时开启“提取兜底”功能和对应模型。打开提取兜底时，此前仅因没有适用提取器而跳过的文件会自动重新入队，点击“保存并开始索引”即可处理。图片理解、语义嵌入、OCR 与 ASR 分别由各自的开关控制。

### 本地 embedding 服务

“语义嵌入”卡片和其他模型一样填写 `base_url`、API Key 与模型名。Semdex 通过 OpenAI 兼容的 `POST <base_url>/embeddings` 调用服务，并读取标准响应中的 `data[].embedding`；LM Studio、Ollama 的兼容模式、vLLM 和 llama.cpp server 等均可接入。对于不提供该协议的本地向量模型，需要先用一个轻量适配服务暴露 OpenAI 兼容接口；当前版本没有单独的任意 HTTP embedding 提供方。

更换 embedding 模型或服务地址后，旧向量不能和新向量混用。保存设置后运行一次完整重建，或执行：

```bash
uv run semdex embed --rebuild
```

在重建完成前，语义搜索会被安全地暂停，混合搜索会退回关键词搜索。

## 4. OCR

### Tesseract

选择“Tesseract（本地命令）”，安装依赖后启用即可：

```bash
brew install tesseract poppler
# 中文识别还需要 chi_sim 语言包；具体安装方式取决于 Homebrew 配方
```

`pdftoppm` 用于把扫描 PDF 转成逐页图片。图片和扫描 PDF 都会使用同一套 OCR 设置；设置页中的超时同时限制 Tesseract、`pdftoppm` 和本地 HTTP OCR 请求。

### PaddleOCR 或其他本地 HTTP 服务

选择“本地 HTTP（PaddleOCR 等）”后，Semdex 会向填写的地址发送 `POST multipart/form-data`：

| 字段 | 内容 |
|---|---|
| `file` | 图片二进制（扫描 PDF 会逐页上传） |
| `languages` | 设置中的 OCR 语言字符串 |
| `Authorization` | 仅填写 API Key 时发送 `Bearer <key>` |

服务应返回 JSON。通过“响应文本路径”指定文本所在的点路径，例如：

```json
{"result": {"text": "识别出的文字"}}
```

上例要填写 `result.text`。这是一层通用适配协议，方便用 FastAPI/Flask 将 PaddleOCR、PaddleX 或其他本地 OCR 推理服务包装成一个小接口；不同版本的 PaddleOCR 原生服务接口并不完全相同，因此需要在包装层转换为上述输入输出。

## 5. 语音识别与视频转写

### faster-whisper 本地运行

选择“faster-whisper（本地）”后先安装可选依赖：

```bash
uv sync --extra asr
```

填写模型名（例如 `base`、`small` 或本地模型目录）、设备和计算精度。第一次加载模型可能下载权重；音视频内容在本机转写。

### OpenAI 兼容 Whisper 接口

选择“OpenAI 兼容接口”后，填写接口基地址，Semdex 默认请求：

```text
<base_url>/audio/transcriptions
```

也可以填写完整“转写接口”来覆盖它。请求为标准 multipart：`file`、`model` 和可选 `language`，可用于本地部署的 Whisper、faster-whisper 服务或兼容的转写网关。默认从响应 JSON 的 `text` 字段读取转写内容；非标准服务可用“响应文本路径”指定如 `data.text`。

## 6. 常见工作流

首次索引：设置目录后点击“保存并开始索引”，或运行 `uv run semdex index`。

视觉或提取兜底模型暂时未启动：依赖它的文件会进入 `waiting_model`；启动服务并重新索引即可补上。实体抽取会保留在独立的等待状态；embedding 服务暂时不可用不会影响全文索引，恢复后运行 `uv run semdex embed` 补向量即可。

OCR/ASR 环境尚未安装：文件会进入 `waiting_capability`；安装或启动本地服务后重新索引即可。

修改目录或排除规则：保存后运行一次增量索引。移除某个索引目录后，下次扫描会清理该目录在影子索引中的记录，原始文件不会被删除。

完整重建：网页状态栏中的“完整重建”会重新提取全部已索引文件。适合改变抽取规则、OCR/ASR 结果或 embedding 模型后使用。

## 7. 命令行与 API

```bash
uv run semdex status
uv run semdex index
uv run semdex search "项目预算"
uv run semdex ask "上个月和张三有关的 PDF 在哪"
uv run semdex entities
uv run semdex embed --rebuild
```

网页服务也提供 REST API。`GET /api/settings` 不会返回密钥，`PUT /api/settings` 会保存并热加载设置；其他接口见 README 的接口表。

## 8. 手工配置与扩展

网页覆盖常用设置，配置文件仍可以手工编辑：

```bash
uv run semdex config
```

任意文件格式可以通过 `[[extractors.rules]]` 关联自定义脚本。脚本只收到受保护的临时快照路径，标准输出会作为可索引文本。更多提取器扩展说明见 README。

当模型或本地能力的 `base_url` / `endpoint` 指向非回环地址时，对应文件内容、图片、音视频或查询会发送给那个服务。处理敏感目录时，请只填写你信任的本地服务地址。
